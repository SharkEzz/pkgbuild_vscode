# tree-sitter-bash ships an `install: node-gyp-build` script for its native binding.
# We only ever consume its prebuilt .wasm, so no package needs to run postinstall.
enable-pre-post-scripts=false
link-workspace-packages=true
